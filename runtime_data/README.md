# Runtime data

This directory contains structured stores queried by agent tools rather than embedded by Graph-RAG. The Axial earthquake MCP server uses the indexed SQLite event database here. Compressed JSONL files are portable full-catalog exports.

Graph-RAG inputs remain under `../data`; original source material remains under `../source_material`.

